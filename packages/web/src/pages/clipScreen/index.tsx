import React, { useEffect, useRef, useState } from "react";
import styles from "./index.module.scss";

const ClipScreen = () => {
	useEffect(() => {}, []);

	return <div id="clipScreen" className={styles.clipScreen}></div>;
};

export default ClipScreen;
